__pycache__/
*.pyc
.DS_Store
Thumbs.db
.vscode/
.idea/
*.log

# Local dev SQLite DB (Turso re-architecture) — never commit; raw data is private
*.db
_smoke_local.db

# Local secrets (Turso creds for running scrapers/scorers from the IDE)
.env
.env.*
!.env.example

# Saved reference pages used to inspect source HTML structure (not needed by tests)
scrapers/fixture/
scrapers/fixtures/

# Generated rating artifacts — regenerated by score_history.py /
# build_dashboard.py / enrich_oneday_dates.py; never commit (huge, churn-y).
data/rider_ratings.json
data/oneday_dates.json
rider_dashboard.html

# Throwaway local spike (relaxes TLS verification — must never ship)
scrapers/spike_history.py
